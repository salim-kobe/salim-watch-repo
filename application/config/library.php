<?php

// Paramètres de configuration de la library


/*
 * Si true, l'autoloader ne générera pas d'erreur fatale si la classe ne peut pas
 * être chargé
 *
 * Les autres library peuvent ensuite installer leurs propres autoloader et essayer de trouver
 * le fichier de classe à charger
 */
$config['autoload-chain'] = false;

// Liste de toutes les classes d'intercepting filters'
$config['intercepting-filters'] = [ ];

